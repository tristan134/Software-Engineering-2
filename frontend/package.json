{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:api": "openapi-generator-cli generate -i http://localhost:8000/openapi.json -g typescript-fetch -o src/generated --additional-properties=supportsES6=true,typescriptThreePlus=true",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.28.0",
    "vite": "^7.2.4",
    "prettier": "^3.0.0"
  }
}
